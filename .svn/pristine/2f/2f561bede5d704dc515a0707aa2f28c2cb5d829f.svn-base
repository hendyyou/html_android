var moreRecentbrowsePage = {
    initialize : function(tabIndex) {
        Utils.bindFooterTabEvent("more_recentbrowse_page", tabIndex);

        if (!Utils.isLogin()) {
            Utils.changeToUserLogin(moreRecentbrowsePage.initData);
        } else {
            moreRecentbrowsePage.initData();
        }

    },

    initData : function() {
        console.log("moreRecentbrowsePage");
    }

};
